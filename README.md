# GensynRPA - Gensyn Faucet Automation

Автоматизация ежедневного клейма токенов с крана [Gensyn Testnet](https://www.alchemy.com/faucets/gensyn-testnet).

## Возможности

- Интеграция с AdsPower API (управление браузерными профилями)
- Stealth-автоматизация через Patchright (patched Playwright)
- Синхронизация с Google Sheets (чтение профилей, запись результатов)
- Автоматический cooldown 24 часа между запросами
- Retry-логика при ошибках (CAPTCHA, таймауты)
- Подробное логирование

## Требования

- Python 3.10+
- [AdsPower](https://www.adspower.com/)
- Google Cloud сервисный аккаунт

## 🛠 Установка

```bash
# Клонировать/скопировать проект
cd GensynRPA

# Установить зависимости
pip install -r requirements.txt

# Установить браузер для Patchright
patchright install chromium
```

## ⚙️ Настройка

### 1. Google Sheets

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите **Google Sheets API**
3. Создайте **Service Account** и скачайте JSON-ключ
4. Переименуйте ключ в `credentials.json` и положите в папку проекта
5. Расшарьте вашу Google таблицу на email сервисного аккаунта

### 2. Google Таблица

Создайте таблицу со следующей структурой:

| A (Profile Number) | B (Address) | C (date_work) | D (yes/no_work) | E (kol-vo_zapros) | F (STATUS) |
|---|---|---|---|---|---|
| 1 | 0xe1e02f1bcccbf3588... | 25.12.2025 14:56 | no | 5 | OK |
| 2 | 0xa2b3c4d5e6f7... | | yes | 0 | |

### 3. Конфигурация

Отредактируйте `config.yaml`:

```yaml
# AdsPower Configuration
adspower:
  api_url: "http://local.adspower.net:50325"

# Google Sheets Configuration
google_sheets:
  credentials_file: "credentials.json"
  spreadsheet_name: "GensynFaucet"  # Имя вашей таблицы
  worksheet_name: "Sheet1"

# Automation Settings
automation:
  retry_count: 3        # Попыток при ошибке
  cooldown_hours: 24    # Часов между запросами
```

## 🚀 Запуск

```bash
python main.py
```

## 📊 Как это работает

1. Загружает конфигурацию из `config.yaml`
2. Подключается к Google Sheets
3. Обновляет колонку `yes/no_work` на основе прошедшего времени
4. Для каждого профиля с `yes`:
   - Открывает браузер AdsPower по `serial_number`
   - Переходит на страницу
   - Вводит адрес кошелька
   - Нажимает "Send 0.1 ETH"
   - Проверяет результат (успех/ошибка)
   - Записывает результат в таблицу
   - Закрывает браузер
5. Выводит итоговую статистику